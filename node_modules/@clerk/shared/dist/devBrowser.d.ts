declare const DEV_BROWSER_SSO_JWT_PARAMETER = "__dev_session";
declare const DEV_BROWSER_JWT_MARKER = "__clerk_db_jwt";
declare function setDevBrowserJWTInURL(url: URL, jwt: string, opts?: {
    hash: boolean;
}): URL;
/**
 * Extract & strip existing jwt from hash
 * Side effect: Removes dev browser from the url hash
 **/
declare function extractDevBrowserJWTFromURLHash(url: URL): string;
/**
 * Extract & strip existing jwt from search params
 * Side effect: Removes dev browser from the search params
 **/
declare function extractDevBrowserJWTFromURLSearchParams(url: URL): string;

export { DEV_BROWSER_JWT_MARKER, DEV_BROWSER_SSO_JWT_PARAMETER, extractDevBrowserJWTFromURLHash, extractDevBrowserJWTFromURLSearchParams, setDevBrowserJWTInURL };
